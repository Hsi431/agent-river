# HANDOFF 2026-07-06 — agent-river v3 完成,待 push

## 一句話
把 agent-river 從「沒人用的 Telegram 遙控器」重造成「本機多 agent 協作匯流排 + 觀察看板」:agent 之間能在 owner 不在場時互相求援(含反向找 Claude),owner 用看板看直播、按少數按鈕取代逐步審批。13 個功能單全上線,376 測試綠,分支 `v3-session` 乾淨,**尚未 push**(等 owner)。

## 現狀
- 分支 `v3-session`(自 v2-phase1 `bf7c9dd` 切),18 commit,相對 v2-phase1 +7.2k/-9.3k 行。
- live:`codex-agent-dashboard.service` 跑最終 code(重啟驗證無斷、NRestarts=0),舊 `codex-agent-telegram-bridge.service` 已停用退役。opus/codex/exec runner timer 正常。
- 版本 package.json 0.3.0,CHANGELOG 已寫;**未 push、未開 PR、未合 main**(鐵律)。

## 做了什麼(單元史)
- **U1** session core:sessions.jsonl 帳本、預算、runner 認 session、agent 發起(唯讀+小額)。
- **U2** 看板 bot 取代舊 bridge(同 token):直播 feed + 五控制件。
- **U3** 握手:agent-join(poll+token)、per-agent token(0600、timingSafeEqual)、spawn 收編。
- **U4** 瘦身:退役 v1 聊天/分類器/舊 Telegram 面 -9.3k 行(裁決在 WORKORDER_U4_KILLLIST.md;cutover 已做)。
- **U5/U6** 易用性:/session 具體錯誤、topic 分級(owner 5/agent 20)、破折號容錯、/model claude|codex。
- **U7** exec 一行接入:`agent-join --style exec --exec '<cmd>'`,stdin 進 stdout 出,任何 agent 可接。
- **U8** 開球+接力:owner 開場自動廣播 topic,agent 回覆自動轉寄下一位,預算是唯一終結者。
- **U9** 可靠性+收割:失敗推播帶錯誤原文、/say 插話、收場寫逐字稿落檔+推最後結論全文、開場去重。
- **U10** session→edit task:收場一鍵轉 workspace-write edit task,走既有 owner 核准,討論→動手閉環。
- **U11** 包裝:`init` 一鍵起全套、quickstart、bin alias、v0.3.0+CHANGELOG。
- **U12** 預算只算 agent 發言(owner/relay 不扣)、feed 不推 owner 訊息、session repo 真的綁進 runner cwd(清「以 agent-river 為中心」遺毒)。
- **U13** push 前安全加固(盲審四 MAJOR):exchange-release token gate、runner cwd 驗 workspace_root+fail-closed、exec env 白名單、v2 outbox redact。
- 參與者 system prompt 模板:docs/AGENT_PROMPT_TEMPLATE.md(卡住協議+注入衛生+用法)。

## 分工與品質
規劃/驗收/審查全程 Fable 親做(owner 硬規則);實作外包 codex(gpt-5.5 **high**,非 xhigh——owner 拍板,反正 Fable 把關)。每單親跑測試+讀真 diff 才 commit。審出並修掉:skip 無界寫入、gateway 名單誤當 owner、測試時鐘炸彈、gpt5.4 殘留 API 400、U13 四個安全邊界。U13 因 codex 額度耗盡由 Fable 親自實作+測。

## 接手要做的
1. **push 決策(等 owner)**:`git push origin v3-session`,要不要開 PR 合 main 一起問。
2. backlog(未做,非阻塞):jsonl 記憶體放大+帳本輪替(MINOR 合併)、runner 節奏旋鈕(90s 太慢時)、成本可見(opus runner 燒 Claude 額度)、定時 session。
3. otter/openclaw 實際接入:各寫一段輪詢 loop 或 exec 入口(otter repo 的活),流程見 README「接入任意 agent」。

## 派工紀律備忘
codex 會突然額度耗盡(U13)或斷網(U3),也曾卡死數小時(U5)。每次派工後排 ~20 分鐘活性鬧鐘;卡死先殺殘骸(含孤兒行程)再重派;額度耗盡就 Fable 自己上(小單)或等重置。
